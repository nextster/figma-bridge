# Contributing

Use Node.js 22.13 or newer. Keep bridge commands narrow and documented, add protocol tests for every command, and run `npm run verify` before opening a change. Do not add tokens, Figma files, generated plugin bundles, or local state to the repository.
